package core

// Package core provides the business logic for escaping template
// delimiters in files. It handles file processing and checksum
// validation to ensure files are only updated when needed.
